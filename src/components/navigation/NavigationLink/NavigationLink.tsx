export type NavigationLinkProps = {
  ariaLabel: string;
  isExternal: boolean;
  href: string;
  text: string;
};

export default function NavigationLink({
  ariaLabel,
  isExternal,
  href,
  text,
}: NavigationLinkProps) {
  return <div></div>;
}
