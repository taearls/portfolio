name = "portfolio-contact-form"
main = "src/index.ts"
compatibility_date = "2024-11-28"

[dev]
port = 8788

[vars]
ALLOWED_ORIGINS = "https://tylerearls.com,http://localhost:5173,http://localhost:3000,http://localhost:4173"
RECIPIENT_EMAIL = "tyler.a.earls@gmail.com"
RATE_LIMIT_MAX = "5"
RATE_LIMIT_WINDOW_SECONDS = "3600"

# Secrets (set via wrangler secret put):
# - POSTMARK_SERVER_TOKEN
# - TURNSTILE_SECRET_KEY

# KV namespace for rate limiting (create with wrangler kv:namespace create "RATE_LIMIT")
# [[kv_namespaces]]
# binding = "RATE_LIMIT"
# id = "<production-kv-id>"
# preview_id = "<preview-kv-id>"
